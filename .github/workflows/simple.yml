name: Shell Commands

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: ecco a string
        run: ecco "Hello world"
      - name: multi line script
        run: |
          node -v
          npm -v